# Relaxed peer-dependency resolution so installs succeed across the
# three.js / model-viewer ecosystem (overlapping peer ranges).
# Vercel and `npm install` read this automatically.
legacy-peer-deps=true
